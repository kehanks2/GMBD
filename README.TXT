This is the project.
I have it set up in VS Code and git has been installed so that we can easily push changes to the repo.

After you make a change to a file go to Source Control on the left panel, or Ctrl + Shift + G. 
Next, click the + next to each file you changed.
Then, click the checkmark at the top of the panel and type in the commit message to push the commit.
Finally, Click the 