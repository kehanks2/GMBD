To Run
-------
The code is ran on the virtual machine given by Kennesaw State University; VMware Horizon Client.

To login use:
Username – GGteam1@win.kennesaw.edu

Password – admin@GGteam1

Then you can run XAMPP if it is not already running from the pinned application on the home bar. Then clicling start Apache and MySQL.

Now you can go to a browser and search : localhost:8080 and the website will pull up.

-------------------------


This is the project folder.

I have everything set up in VS Code including git,
which has been installed so that we can easily push changes to the repo.

------------------------------------------------------------------------------------------------------

1. After you make a change to a file go to Source Control on the left panel, or Ctrl + Shift + G. 

2. Next, click the + next to each file you changed.

3. Then, click the checkmark at the top of the panel and type in the commit message to push the commit.

4. Finally, Click the second button on the bottom blue task bar (should be to the right of main
and show ( 0 | 1 ).). 

------------------------------------------------------------------------------------------------------

That's it!!
